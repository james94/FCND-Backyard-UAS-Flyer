I have done research into Shield AI, which works on UAS Drone tasks. They provide an end-to-end system where drones can take lift off fly, land on the ground and fly across 3D open air space in the sky and perform all sorts of tasks. I am currently working on building a software robotic simulation infrastructure and application in C++ developing with the industry standard ROS2 middleware, ROS2 MoveIt2 autonomy, ROS2 workflow event driven state machines (SMACC2) and Docker Images/Containers for smoother development environments where we can develop our ROS2 software infrastructure that interacts with our Unity3D simulator to command and control the UAS drone. Typically from the backend, we leverage "UdaciDrone API" python library to interact with the Unity3D UAS drone simulator, but for our case we're leveraging ROS2, can you parse through my forked "/home/ubuntu/src/forked/udacidrone" local github repo, so we can understand the "udacidrone" python code and then create a ROS2 C++ driver package for "UdaciDrone API"? I would also imagine some of the modes for ROS2 on the backend interacting with the Unity3D UAS drone simulator where there could be a mission mode where a UAS drone flies across the 3D open air space from starting position to target position with the waypoints illustrating a rectangle shape, another ROS2 mission mode with the Unity3D UAS drone simulator could be requesting the drone flies across waypoints illustrating a star, since the UAS drone is flying across our neighbors backyards another mission mode could be flying across waypoints to act as a surveillance and watch over the neighbors backyards, etc. Can you analyze this information I just shared with you about the ROS2 C++ backend software infrastructure interacting with the Unity3D UAS drone simulation software, then can you analyze the original task we were given from Udacity's "FCND-Backyard-UAS-Flyer" project's "/home/ubuntu/src/FCND-Backyard-UAS-Flyer/README.md" file, then from all the information and knowledge you gather, can you create a software design and development problem statement that we can follow step by step to reproduce and develop our own modular ROS2 C++ backend application that can interact with the Unity3D UAS drone simulator and add the writeup in "/home/ubuntu/src/FCND-Backyard-UAS-Flyer/research/ros2_cpp_research/COPILOT_SOFTWARE_PROBLEM_STATEMENT.md" file?

If you need more information on ROS2 middleware, can you parse my forked "examples" local github ROS2 repo? If you need more information on ROS2 MoveIt2 autonomy, can you parse my forked "moveit2_tutorials" local github ROS2 repo? If you need more information on ROS2 workflow event driven state machine, can you parse my forked "SMACC2" local github ROS2 repo? Then from the information and knowledge you gather, can you incorporate into our "/home/ubuntu/src/FCND-Backyard-UAS-Flyer/research/ros2_cpp_research/COPILOT_SOFTWARE_PROBLEM_STATEMENT.md" file?
